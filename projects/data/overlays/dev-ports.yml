# Development host ports for the data tier.
# Use only with dev.cmd / IDE workflows.
services:
  postgres:
    ports: ["${LOCAL_PG_HOST_BIND:-127.0.0.1}:5432:5432"]
  valkey:
    ports: ["${LOCAL_VALKEY_HOST_BIND:-127.0.0.1}:6379:6379"]
