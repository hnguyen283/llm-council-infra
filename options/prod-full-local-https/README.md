# prod-full-local-https

Production-like local stack with the Portal UI built into an Nginx edge and
served at `https://localhost:8443`.

Requires `cert.pem` and `key.pem` under `llm-council-infra/ssl` or a custom
`HTTPS_CERT_DIR`.
